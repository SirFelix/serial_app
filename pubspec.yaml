name: serial_app
description: "An App for Monitoring Serial Output from a device"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_libserialport: ^0.5.0
  syncfusion_flutter_charts: ^30.1.39

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
